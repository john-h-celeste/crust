#include <iostream>
using std::cout;
using std::endl;

int main() {
    // The result of computation of
    // (10 + 123) * 7 / (23 - 20 - 3) is a runtime error
    //cout << (10 + 123) * 7 / (23 - 20 - 3) << endl;

    // Display the cube of 2
    cout << "Cube of 2 is " << 2 * 2 * 2 << endl;

    return 0;
}